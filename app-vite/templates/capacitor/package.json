{
  "name": "<%= scope.pkg.name %>",
  "version": "<%= scope.pkg.version %>",
  "description": "<%= scope.pkg.description %>",<% if (scope.pkg.author) { %>
  "author": <%= JSON.stringify(scope.pkg.author) %>,<% } %>
  "private": true,
  "dependencies": {
    "@capacitor/app": "^8.0.0",
    "@capacitor/cli": "^8.0.0",
    "@capacitor/core": "^8.0.0"
  }
}
